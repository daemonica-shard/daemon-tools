apiVersion: 1

datasources:
  # Reached over the compose network, not through Caddy: no TLS, no ingest token, and it keeps
  # working if the public hostname is ever down.
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
