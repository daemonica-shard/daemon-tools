# Inherits everything from the stock login theme; we only add a stylesheet.
# `styles` replaces rather than appends, so the parent's css/login.css must be listed too.
parent=keycloak
import=common/keycloak
styles=css/login.css css/daemonica.css
